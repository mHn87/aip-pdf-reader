# MinerU (embedded in code)

# Supabase / PostgreSQL (required for Jobs & AIPs)
# DATABASE_URL="postgresql://...?pgbouncer=true"   (pooler, port 6543)
# DIRECT_URL="postgresql://...:5432/..."           (direct, for migrations)
